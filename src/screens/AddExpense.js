import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  useColorScheme,
  View,
} from 'react-native';
import {Text} from 'react-native-paper';

const AddExpense = () => {
  return (
    <ScrollView>
      <Text>Saazish ma shamil saara jahaan</Text>
    </ScrollView>
  );
};

export default AddExpense;
