import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  useColorScheme,
  View,
} from 'react-native';
import {Text} from 'react-native-paper';

const profile = () => {
  return (
    <ScrollView>
      <Text>profile 1</Text>
    </ScrollView>
  );
};

export default profile;
