import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  useColorScheme,
  View,
} from 'react-native';
import {Text} from 'react-native-paper';

const Categories = () => {
  return (
    <ScrollView>
      <Text>Categ</Text>
    </ScrollView>
  );
};

export default Categories;
